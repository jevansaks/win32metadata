# eaptypes.h — accepted-normalized

**Partition:** EapHost
**Size:** ~30836 bytes

## Analysis

`eaptypes.h` (EAPHost supplicant interaction types) declares only
constants (`eapProp*` flags) and structs (`EAP_ERROR`, etc.). It
contains **no function declarations**.

## Conclusion

Clean. No patch required.
