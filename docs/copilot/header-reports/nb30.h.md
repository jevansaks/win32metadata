# nb30.h — accepted-normalized

**Partition:** NetBios
**Size:** ~13204 bytes

## Analysis

`nb30.h` declares the portable NetBIOS 3.0 API: a single function
`UCHAR APIENTRY Netbios(PNCB pncb)` operating on the transparent `NCB`
(Network Control Block) struct passed by pointer. No opaque handle
types are produced.

## Conclusion

Clean. No opaque handle production. No patch required.
