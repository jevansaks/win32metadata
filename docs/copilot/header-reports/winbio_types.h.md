# winbio_types.h — accepted-normalized

**Partition:** SecBitomet
**Size:** ~91631 bytes

## Analysis

`winbio_types.h` (Windows Biometrics type definitions, constants,
structures) declares only types/constants/structs. It contains **no
function declarations**.

## Conclusion

Clean. No patch required.
