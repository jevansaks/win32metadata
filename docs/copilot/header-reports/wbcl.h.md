# wbcl.h — accepted-normalized

**Partition:** Qos
**Size:** ~47456 bytes

## Analysis

`wbcl.h` (definitions and prototypes for parsing the TCG Log)
declares only constants/structs (`SIPAEV_*` event types, TCG PCR
event structures). It contains **no function declarations**.

## Conclusion

Clean. No patch required.
