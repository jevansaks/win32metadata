# Header Report: dsconf.h

## Partitions
`Audio.DirectMusic`

## Ownership audit (producer-site-only policy)
- Pure DirectSound configuration GUID constants and property-ID enums (`DSPROPERTY_DIRECTSOUNDDEVICE`). No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants/enum only, no functions).
