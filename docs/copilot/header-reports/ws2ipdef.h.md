# ws2ipdef.h — accepted-normalized

**Partition:** IpHlp, WinSock
**Size:** ~32917 bytes

## Analysis

`ws2ipdef.h` (WinSock2 TCP/IP protocol-specific annex definitions)
declares only structs and constants. It contains **no function
declarations**.

## Conclusion

Clean. No patch required.
