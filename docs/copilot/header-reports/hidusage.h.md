# hidusage.h

**Classification:** accepted-normalized (no producer-site change needed)

## Summary
Public HID (Human Interface Device) Usage Page/Usage constant
definitions per USB HID Usage Tables 1.6 (`#define` macros only, kernel
& user mode). No callable functions declared in this header.

## Ownership Analysis
No functions exist, so no producer/consumer handle relationship
applies. No `autoTypes.json`/`emitter.settings.rsp` changes required.

## Action
None. Classified accepted-normalized with no patch artifact.
