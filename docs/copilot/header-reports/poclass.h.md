# poclass.h — accepted-normalized

**Partition:** Power
**Size:** ~25378 bytes

## Analysis

`poclass.h` (power policy device driver interfaces) declares only
`DEFINE_DEVPROPKEY`-based property-key constants. It contains **no
function declarations** (0 `WINAPI`/`STDAPI` matches).

## Conclusion

Clean. No patch required.
