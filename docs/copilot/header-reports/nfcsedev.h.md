# Header Report: nfcsedev.h

## Partitions
`Nfc`

## Ownership audit (producer-site-only policy)
- Pure device-interface GUID constant, `IOCTL_NFCSE_*` control-code constants, and enums/structs for the NFC secure-element device driver interface. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (GUID/IOCTL constants and structs only, no functions).
