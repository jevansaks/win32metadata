# Header Report: dot1x.h

## Partitions
`Ndis`, `NWifi`

## Ownership audit (producer-site-only policy)
- Declares only 802.1X authentication enums/structs (`ONEX_AUTH_IDENTITY`, `ONEX_AUTH_STATUS`, etc.). No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (enums/structs only, no functions).
