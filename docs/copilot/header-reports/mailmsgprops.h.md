# mailmsgprops.h — accepted-normalized

**Partition:** IMapi
**Size:** ~27101 bytes

## Analysis

`mailmsgprops.h` declares only `IMMPID_*` property-ID enum macros
(`IMMPID_START_LIST`/`IMMPID_END_LIST`) for SMTP/NNTP MailMsg
properties. It contains **no function declarations, no structs, no
handle typedefs**.

## Conclusion

Clean. No patch required.
