# inetreg.h — accepted-normalized

**Partition:** InternetExplorer
**Size:** ~24691 bytes

## Analysis

`INETREG.H` (registry-key string literals) declares only `TSZ*`/
`REGSTR_PATH_*` string constants. It contains **no function
declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
