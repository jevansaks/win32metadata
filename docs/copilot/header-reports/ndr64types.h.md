# ndr64types.h — accepted-normalized

**Partition:** Rpc
**Size:** ~22160 bytes

## Analysis

`ndr64types.h` (NDR64 format-string definitions) declares only
transparent type definitions (`NDR64_UINT8`, etc.) and constants. It
contains **no function declarations** (0 `WINAPI`/`STDAPI` matches).

## Conclusion

Clean. No functions, no opaque handle production. No patch required.
