# exdispid.h — accepted-normalized

**Partition:** InternetExplorer
**Size:** ~12491 bytes

## Analysis

`exdispid.h` declares only `DISPID_*` constants for
`IExplorer`/`WebBrowser` dispatch events. It contains **no function
declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
