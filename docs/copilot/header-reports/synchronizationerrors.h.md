# Header Report: synchronizationerrors.h

## Partitions
`WinSync`

## Ownership audit (producer-site-only policy)
- Pure Microsoft Synchronization Platform HRESULT error-code `#define` constants. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
