# vssym32.h — accepted-normalized

**Partition:** Controls
**Size:** ~15284 bytes

## Analysis

`vssym32.h` (Visual Styles Core Win32/ComCtl32 symbols) declares only
transparent C-style enums (`BGTYPE`, `IMAGELAYOUT`, `BORDERTYPE`,
etc.). It contains **no function declarations** (0
`WINAPI`/`STDAPI`/`__cdecl` matches).

## Conclusion

Clean. No patch required.
