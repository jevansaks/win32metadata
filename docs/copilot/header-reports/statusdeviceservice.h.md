# Header Report: statusdeviceservice.h

## Partitions
`WpdSdk`

## Ownership audit (producer-site-only policy)
- Pure device-service GUID/property-key constants (`DEFINE_DEVSVCGUID`/`DEFINE_DEVSVCPROPKEY`) and `#define` name/range/enum constants for WPD status device service properties. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
