# Header Report: functiondiscoverycategories.h

## Partitions
`FunctionDiscovery`

## Ownership audit (producer-site-only policy)
- Declares only `FCTN_CATEGORY_*`/`FCTN_SUBCAT_*`/`FD_SUBKEY*` string constants. No functions, no handles.

## Conclusion
`accepted-normalized` — no ownership annotation required (string constants only, no functions).
