# Header Report: fullenumsyncdeviceservice.h

## Partitions
`WpdSdk`

## Ownership audit (producer-site-only policy)
- Pure device-service GUID/property-key constants (`DEFINE_DEVSVCGUID`/`DEFINE_DEVSVCPROPKEY`) for the WPD Full Enumeration Sync device service. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
