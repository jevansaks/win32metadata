# evcode.h — accepted-normalized

**Partition:** Media.DShow
**Size:** ~21902 bytes

## Analysis

`EvCode.h` (DirectShow/Quartz standard event codes) declares only
`EC_*` constants. It contains **no function declarations, no
structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
