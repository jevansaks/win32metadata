# functiondiscoverykeys_devpkey.h — accepted-normalized

**Partition:** Audio
**Size:** ~19479 bytes

## Analysis

`devpkey.h` (Plug and Play Device Property API property keys)
declares only `DEFINE_PROPERTYKEY`-based `PKEY_*` constants. It
contains **no function declarations, no structs, no handle
typedefs**.

## Conclusion

Clean. No patch required.
