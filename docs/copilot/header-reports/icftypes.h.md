# Header Report: icftypes.h

## Partitions
`WindowsFirewall`

## Ownership audit (producer-site-only policy)
- Pure Windows Firewall (`NET_FW_*`) enum type definitions plus MIDL boilerplate. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (enums + MIDL boilerplate only, no functions).
