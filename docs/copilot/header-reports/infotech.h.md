# infotech.h — accepted-normalized

**Partition:** HtmlHelp
**Size:** ~26127 bytes

## Analysis

`infotech.h` declares GUID constants and a C++ class (`CProperty`).
It contains **no `extern`/`DllImport` function declarations** (0
`STDAPI` matches) — C++ classes are not scrapable/emittable
declarations.

## Conclusion

Clean. No scrapable functions, no opaque handle production. No patch
required.
