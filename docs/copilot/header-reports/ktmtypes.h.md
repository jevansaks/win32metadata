# Header Report: ktmtypes.h

## Partitions
`Fs`

## Ownership audit (producer-site-only policy)
- Pure KTM (Kernel Transaction Manager) type aliases (`UOW`/`CRM_PROTOCOL_ID` as `GUID`) and `TRANSACTION_MANAGER_*`/`TRANSACTION_*`/`RESOURCE_MANAGER_*` option constants. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (type aliases/constants only, no functions).
