# dvp.h — accepted-normalized

**Partition:** DirectDraw
**Size:** ~33385 bytes

## Analysis

`dvp.h` (DirectDrawVideoPort include file) declares only GUID
constants (`DEFINE_GUID`) and transparent structs used by the
`IDirectDrawVideoPort` COM interfaces (declared elsewhere). It
contains **no direct function declarations**.

## Conclusion

Clean. No patch required.
