# Header Report: ntddkbd.h

## Partitions
`Hid`

## Ownership audit (producer-site-only policy)
- Pure keyboard-device-name string constants and `IOCTL_KEYBOARD_*` control-code constants. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
