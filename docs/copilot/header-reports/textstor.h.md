# textstor.h — accepted-normalized

**Partition:** Tsf
**Size:** ~116550 bytes

## Analysis

MIDL-generated COM header (Text Store `ITextStoreACP` interfaces).
Confirmed via multi-line-aware grep that there are **no free function
declarations** — all methods are COM vtable methods, out of scope
per blocker-class 5.

## Conclusion

Clean. No patch required.
