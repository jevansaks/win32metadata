# Header Report: functiondiscoveryconstraints.h

## Partitions
`FunctionDiscovery`

## Ownership audit (producer-site-only policy)
- Pure Function Discovery query-constraint name/value string constants (`FD_QUERYCONSTRAINT_*`/`FD_CONSTRAINTVALUE_*`, `PROVIDERPNP_QUERYCONSTRAINT_*`). No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
