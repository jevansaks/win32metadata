# wheadef.h — accepted-normalized

**Partition:** Debug
**Size:** ~30234 bytes

## Analysis

`wheadef.h` (Windows Hardware Error Architecture) declares only
structs/constants (`WHEA_ERROR_SOURCE_TYPE`, error-record
descriptors). It contains **no function declarations**.

## Conclusion

Clean. No patch required.
