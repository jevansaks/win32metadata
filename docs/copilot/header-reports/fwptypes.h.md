# fwptypes.h — accepted-normalized

**Partition:** WindowsFilteringPlatform
**Size:** ~17882 bytes

## Analysis

`fwptypes.h` is MIDL-generated (Windows Filtering Platform base type
COM/RPC interfaces). It contains **no free functions** (`STDAPI`
count: 0) — all methods (if any) are COM vtable methods, out of scope
per the COM-vtable-methods convention (blocker-class 5).

## Conclusion

Clean. No patch required.
