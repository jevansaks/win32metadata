# Header Report: stgprop.h

## Partitions
`Search`

## Ownership audit (producer-site-only policy)
- Declares only `PID_STG_*`/`PSGUID_STORAGE`/`CSTORAGEPROPERTY` PROPID/GUID constants. No functions, no handles.

## Conclusion
`accepted-normalized` — no ownership annotation required (PROPID/GUID constants only, no functions).
