# Header Report: ehstormsg.h

## Partitions
`Enstor`

## Ownership audit (producer-site-only policy)
- Pure Enhanced Storage error-code range constants (`ES_*_ERROR_START`/`ES_*_ERROR_END`) and error-classification macros. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants/macros only, no functions).
