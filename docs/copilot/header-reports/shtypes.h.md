# shtypes.h — accepted-normalized

**Partitions:** Shell, Shell.Common
**Size:** ~15596 bytes

## Analysis

`shtypes.h` is MIDL-generated (Shell base type definitions, e.g.
`STRRET`, `SHITEMID`). It contains **no free functions** (`STDAPI`
count: 0) — all methods (if any) are COM vtable methods, out of scope
per the COM-vtable-methods convention (blocker-class 5).

## Conclusion

Clean. No patch required.
