# Header Report: msiehost.h

## Partitions
`InternetExplorer`

## Ownership audit (producer-site-only policy)
- Declares only `CGID_InternetExplorer` GUID and `IECMDID_*` command-ID constants. No functions, no handles.

## Conclusion
`accepted-normalized` — no ownership annotation required (GUID/command-ID constants only, no functions).
