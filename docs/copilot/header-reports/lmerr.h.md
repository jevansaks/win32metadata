# lmerr.h — accepted-normalized

**Partition:** NetMgmt
**Size:** ~44489 bytes

## Analysis

`lmerr.h` (LAN Manager network error definitions) declares only
`NERR_*` error-code constants. It contains **no function
declarations**.

## Conclusion

Clean. No patch required.
