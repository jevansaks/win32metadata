# codecapi.h

**Classification:** accepted-normalized (no producer-site change needed)

## Summary
CODECAPI GUID definitions (`DEFINE_CODECAPI_GUID`) for `ICodecAPI` property
identifiers used by Media Foundation transforms. No free-function
declarations found (0 matches for the standard scrape-target pattern);
all behavior is exposed via the existing COM `ICodecAPI` interface
(out of scope per blocker class 5 — COM vtable methods).

## Ownership Analysis
No non-COM producer/consumer functions exist. No `autoTypes.json`/
`emitter.settings.rsp` changes required.

## Action
None. Classified accepted-normalized with no patch artifact.
