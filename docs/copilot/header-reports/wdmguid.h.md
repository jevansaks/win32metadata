# wdmguid.h — accepted-normalized

**Partition:** DevInst
**Size:** ~17512 bytes

## Analysis

`wdmguid.h` (`shared\wdmguid.h`) declares only `DEFINE_GUID`-based
device-class and Plug & Play event GUID constants. It contains **no
function declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
