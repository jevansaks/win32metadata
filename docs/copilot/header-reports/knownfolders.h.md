# knownfolders.h — accepted-normalized

**Partition:** Shell
**Size:** ~26657 bytes

## Analysis

`knownfolders.h` declares only `DEFINE_KNOWN_FOLDER`-based
`FOLDERID_*` GUID constants. It contains **no function declarations,
no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
