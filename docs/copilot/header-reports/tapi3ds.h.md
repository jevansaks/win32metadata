# tapi3ds.h — accepted-normalized

**Partition:** Tapi3
**Size:** ~11048 bytes

## Analysis

`tapi3ds.h` is MIDL-generated (TAPI 3 device-specific extension
interfaces). It contains **no free functions** — all methods are COM
vtable methods, out of scope per the COM-vtable-methods convention
(blocker-class 5).

## Conclusion

Clean. No patch required.
