# prntfont.h — accepted-normalized

**Partition:** Printing
**Size:** ~19105 bytes

## Analysis

`prntfont.h` (printer driver font metrics/glyphset data structures)
declares only transparent structs (UFM/UFF/GTT data). It contains
**no function declarations** (0 `WINAPI`/`STDAPI` matches).

## Conclusion

Clean. No functions, no opaque handle production. No patch required.
