# qnetwork.h — accepted-normalized

**Partition:** Media.DShow
**Size:** ~17735 bytes

## Analysis

`qnetwork.h` (mktyplib-generated `QuartzNetTypeLib` interop) declares
the `IAMNetShowConfig` COM interface via `DECLARE_INTERFACE_`. It
contains **no free functions** — all methods are COM vtable methods,
out of scope per the COM-vtable-methods convention (blocker-class 5).

## Conclusion

Clean. No patch required.
