# sqloledb.h — accepted-normalized

**Partition:** Search
**Size:** ~26234 bytes

## Analysis

`SQLOLEDB.H` declares only provider-specific `CLSID_*`/`IID_*` GUID
constants for the SQL Server OLE DB provider. It contains **no
function declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
