# infstr.h — accepted-normalized

**Partition:** DevInst
**Size:** ~19098 bytes

## Analysis

`infstr.h` (SetupAPI INF string definitions) declares only `SZ_KEY_*`
string constants. It contains **no function declarations, no
structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
