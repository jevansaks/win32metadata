# Header Report: cplext.h

## Partitions
`Shell`

## Ownership audit (producer-site-only policy)
- Declares only `CPLPAGE_*` control-panel property-page ID constants. No functions, no handles.

## Conclusion
`accepted-normalized` — no ownership annotation required (property-page ID constants only, no functions).
