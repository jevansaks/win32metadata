# Header Report: usbiodef.h

## Partitions
`Buses`

## Ownership audit (producer-site-only policy)
- Pure USB IOCTL function-code and control-code `#define` constants for the core USB stack (HCD/HUB/PDO). No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
