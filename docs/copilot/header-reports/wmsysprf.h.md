# wmsysprf.h — accepted-normalized

**Partition:** Wmp
**Size:** ~23095 bytes

## Analysis

`wmsysprf.h` (Windows Media system profile GUIDs) declares only
`EXTERN_GUID`-based profile GUID constants. It contains **no function
declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
