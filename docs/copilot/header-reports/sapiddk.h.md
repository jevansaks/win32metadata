# sapiddk.h

**Classification:** accepted-normalized (no producer-site change needed)

## Summary
Speech API (SAPI) Device Driver Kit — COM interfaces for speech engine
site/token providers (`ISpTokenId`, etc.). All 371 callable declarations
found are `STDMETHOD`/`STDMETHOD_` COM vtable methods — out of scope per
blocker class 5 (COM vtable methods). No top-level free-function
declarations matched the standard scrape-target pattern (0 matches).

## Ownership Analysis
No free (non-COM) producer/consumer functions exist. No
`autoTypes.json`/`emitter.settings.rsp` changes required.

## Action
None. Classified accepted-normalized with no patch artifact.
