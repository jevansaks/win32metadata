# Header Report: d3d9on12.h

## Partitions
`Direct3D9on12`

## Scrape validation
- Re-scraped `Direct3D9on12` partition (`ScanArch=x86`) after touching `main.cpp`.
- Result: `Build succeeded. 0 Warning(s). 0 Error(s).`

## Ownership audit (producer-site-only policy)
- `Direct3DCreate9On12Ex`/`Direct3DCreate9On12` are COM factory functions (`IDirect3D9Ex**`/`IDirect3D9*` return). `IDirect3DDevice9On12::GetD3D12Device`/`UnwrapUnderlyingResource`/`ReturnUnderlyingResource` output COM interface pointers (`void**`/`ID3D12CommandQueue*`/`ID3D12Fence**`) — standard COM refcounting pattern. `D3D9ON12_ARGS` is a plain data struct holding caller-supplied `IUnknown*` pointers. No raw `HANDLE` involved.

## Conclusion
`accepted-normalized` — no ownership annotation required (COM factory/refcounting pattern throughout).
