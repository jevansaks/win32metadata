# Header Report: shdispid.h

## Partitions
`Shell`

## Ownership audit (producer-site-only policy)
- Declares only `DISPID_*` shell-folder-view/search/ADC event constants. No functions, no handles.

## Conclusion
`accepted-normalized` — no ownership annotation required (DISPID constants only, no functions).
