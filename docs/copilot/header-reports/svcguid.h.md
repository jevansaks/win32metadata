# svcguid.h — accepted-normalized

**Partition:** WinSock
**Size:** ~19074 bytes

## Analysis

`svcguid.h` declares only well-known-service GUID constants
(`HOSTNAME`, RFC 1060 services, NetWare services). It contains **no
function declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
