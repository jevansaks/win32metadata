# identityprovider.h — accepted-normalized

**Partition:** IdentityProvider
**Size:** ~64732 bytes

## Analysis

MIDL-generated COM header (Identity Provider interfaces). Confirmed
via multi-line-aware grep that there are **no free function
declarations** — all methods are COM vtable methods, out of scope
per blocker-class 5.

## Conclusion

Clean. No patch required.
