# mcx.h — accepted-normalized

**Partition:** Communication
**Size:** ~24640 bytes

## Analysis

`mcx.h` (32-bit Windows MCX modem APIs) declares only transparent
structs (`MODEMDEVCAPS`, etc.). It contains **no function
declarations** (0 `WINAPI`/`STDAPI` matches).

## Conclusion

Clean. No functions, no opaque handle production. No patch required.
