# ie12plugin.h — accepted-normalized

**Partition:** InternetExplorer
**Size:** ~65916 bytes

## Analysis

MIDL-generated COM header (IE12 plugin interfaces). Confirmed via
multi-line-aware grep that there are **no free function
declarations** — all methods are COM vtable methods, out of scope
per blocker-class 5.

## Conclusion

Clean. No patch required.
