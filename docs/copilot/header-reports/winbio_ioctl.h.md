# Header Report: winbio_ioctl.h

## Partitions
`SecBitomet`

## Ownership audit (producer-site-only policy)
- Pure `BIO_CTL_CODE`-derived IOCTL constants and a device-interface GUID for the biometric device class/port. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (IOCTL/GUID constants only, no functions).
