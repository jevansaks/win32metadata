# Header Report: RTInfo.h

## Partitions
`NetMgmt`

## Ownership audit (producer-site-only policy)
- Declares only router-manager information-block data structs (`RTR_TOC_ENTRY`, `RTR_INFO_BLOCK_HEADER`, etc.). No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (structs only, no functions).
