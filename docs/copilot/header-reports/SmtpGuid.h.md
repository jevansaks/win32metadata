# Header Report: SmtpGuid.h

## Partitions
`IMapi`

## Ownership audit (producer-site-only policy)
- Pure SMTP event GUID/string constants (`DECLARE_EVENTGUID_STRING`/`DEFINE_GUID`). No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (GUID/string constants only, no functions).
