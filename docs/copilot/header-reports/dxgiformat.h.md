# Header Report: dxgiformat.h

## Partitions
`Dxgi`, `Dxgi.Common`

## Ownership audit (producer-site-only policy)
- Pure `DXGI_FORMAT` enum. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (enum only, no functions).
