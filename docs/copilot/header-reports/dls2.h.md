# Header Report: dls2.h

## Partitions
`Audio.DirectMusic`

## Ownership audit (producer-site-only policy)
- Pure DLS2 (Downloadable Sounds) FOURCC/format `#define` constants (`FOURCC_*`, `CONN_SRC_*`, `CONN_DST_*`). No functions, no handles.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
