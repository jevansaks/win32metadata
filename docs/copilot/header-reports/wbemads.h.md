# wbemads.h — accepted-normalized

**Partition:** Wmi
**Size:** ~10973 bytes

## Analysis

`wbemads.h` is MIDL-generated (WMI/ADSI provider interfaces). It
contains **no free functions** — all methods are COM vtable methods,
out of scope per the COM-vtable-methods convention (blocker-class 5).

## Conclusion

Clean. No patch required.
