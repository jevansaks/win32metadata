# recdefs.h — accepted-normalized

**Partition:** Tablet
**Size:** ~15780 bytes

## Analysis

`RecDefs.h` (Tablet PC handwriting recognizer definitions) declares
only `RECOFLAG_*`/`RECOCONF_*`/`GESTURE_*` constants. It contains **no
function declarations, no structs, no handle typedefs**.

## Conclusion

Clean. No patch required.
