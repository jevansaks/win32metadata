# ehstorextensions.h — accepted-normalized

**Partition:** Enstor
**Size:** ~35037 bytes

## Analysis

`EhStorExtensions.h` (Enhanced Storage WPD interfaces for silo
drivers) declares only GUID constants (`DEFINE_GUID`) and transparent
structs (`ENHANCED_STORAGE_PASSWORD_SILO_INFORMATION`). It contains
**no function declarations**.

## Conclusion

Clean. No patch required.
