# aviriff.h — accepted-normalized

**Partition:** Media.DShow
**Size:** ~14120 bytes

## Analysis

`aviriff.h` declares only transparent, word-packed RIFF/AVI file
format structs (`AVIMAINHEADER`, `AVISTREAMHEADER`, etc.). It contains
**no function declarations** (0 `WINAPI`/`STDAPI`/`__cdecl`/`__stdcall`
matches).

## Conclusion

Clean. No functions, no opaque handle production. No patch required.
