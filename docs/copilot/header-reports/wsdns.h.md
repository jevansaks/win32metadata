# wsdns.h — accepted-normalized

**Partition:** WebServicesOnDevices
**Size:** ~20298 bytes

## Analysis

`wsdns.h` (auto-generated WSDAPI namespace definitions) declares only
`extern WSDXML_NAME`/`WSDXML_NAMESPACE` array declarations and
`WSDXML_NAME_ENCODING`-based constants. It contains **no function
declarations** (0 `WINAPI`/`STDAPI` matches).

## Conclusion

Clean. No functions, no opaque handle production. No patch required.
