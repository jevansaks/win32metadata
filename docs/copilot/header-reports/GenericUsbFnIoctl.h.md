# Header Report: GenericUsbFnIoctl.h

## Partitions
`Buses`

## Ownership audit (producer-site-only policy)
- Pure `IOCTL_GENERICUSBFN_*` control-code constants (`CTL_CODE` macro invocations) for the generic USB function class driver. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (IOCTL constants only, no functions).
