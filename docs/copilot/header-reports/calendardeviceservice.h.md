# Header Report: calendardeviceservice.h

## Partitions
`WpdSdk`

## Ownership audit (producer-site-only policy)
- Pure device-service GUID/property-key constants (`DEFINE_DEVSVCGUID`/`DEFINE_DEVSVCPROPKEY`) for the WPD Calendar device service. No functions.

## Conclusion
`accepted-normalized` — no ownership annotation required (constants only, no functions).
