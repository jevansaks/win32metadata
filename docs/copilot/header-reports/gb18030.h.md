# Header Report: gb18030.h

## Partitions
`Intl`

## Ownership audit (producer-site-only policy)
- Single function `NlsDllCodePageTranslation` performs codepage translation into caller-supplied string buffers (`LPSTR`/`LPWSTR`) and a caller-supplied `LPCPINFO` struct — no opaque handle produced.

## Conclusion
`accepted-normalized` — no ownership annotation required (buffer-conversion function only, no handles).
