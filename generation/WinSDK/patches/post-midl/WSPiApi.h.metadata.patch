diff --git a/generation/WinSDK/RecompiledIdlHeaders/um/WSPiApi.h b/generation/WinSDK/RecompiledIdlHeaders/um/WSPiApi.h
index 2627462f..5e2598c2 100644
--- a/generation/WinSDK/RecompiledIdlHeaders/um/WSPiApi.h
+++ b/generation/WinSDK/RecompiledIdlHeaders/um/WSPiApi.h
@@ -18,6 +18,7 @@ Revision History:
 
 #pragma once
 #include <winapifamily.h>
+#include <win32metadata_annotations.h>
 
 #pragma region Desktop Family
 #if WINAPI_FAMILY_PARTITION(WINAPI_PARTITION_DESKTOP)
@@ -778,6 +779,7 @@ Return Value
         if (iFlags & NI_NUMERICSERV)
         {
             // return numeric form of the address.
+_Win32_metadata_supported_os_(windows8.1)
             _WSPIAPI_SPRINTF_S_1(szBuffer, _WSPIAPI_COUNTOF(szBuffer), "%u", ntohs(wPort));
         }
         else
@@ -793,6 +795,7 @@ Return Value
             else
             {
                 // DRAFT: return numeric form of the port!
+_Win32_metadata_supported_os_(windows8.1)
                 _WSPIAPI_SPRINTF_S_1(szBuffer, _WSPIAPI_COUNTOF(szBuffer), "%u", ntohs(wPort));
             }
         }
